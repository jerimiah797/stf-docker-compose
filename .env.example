# STF Central Server Configuration
# Copy this file to .env and customize for your environment

# Public hostname/IP for your STF server (used by web UI and remote providers)
PUBLIC_IP=stf.yourdomain.com

# Shared secret for authentication (CHANGE THIS TO A RANDOM VALUE!)
# Generate with: openssl rand -base64 32
SECRET=change-this-to-a-random-secret-key

# RethinkDB connection string (leave as-is for default setup)
RETHINKDB_PORT_28015_TCP=tcp://rethinkdb:28015

# Station name for the central server (optional, mainly for identification)
STATION_NAME=CENTRAL
