# STF Remote Provider Configuration
# Copy this file to .env and customize for your environment

# Central server's public hostname/IP (where STF web UI is hosted)
PUBLIC_IP=stf.yourdomain.com

# Shared secret (MUST MATCH central server's SECRET)
SECRET=change-this-to-match-central-server-secret

# RethinkDB connection (not used by remote, but required by STF)
RETHINKDB_PORT_28015_TCP=tcp://rethinkdb:28015

# Unique name for this remote station (e.g., REMOTE1, BUILDING_A, LAB2)
# This name will appear in the STF UI for device identification
STATION_NAME=REMOTE1

# This remote machine's public hostname/IP
# Clients will connect directly to this IP for device interactions
STATION_IP=remote1.yourdomain.com
